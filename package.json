{
  "name": "facepulse",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A local, experimental camera heart-rate lab using POS rPPG",
  "scripts": { "start": "node server.mjs", "test": "node --test tests/*.test.mjs" },
  "engines": { "node": ">=18" }
}
